<launch>

  <arg name="map_file" default="$(find light_scan_sim)/test/office.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />

</launch>
